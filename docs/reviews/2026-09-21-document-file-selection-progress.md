# 决议与修正案文件接入：阶段记录

日期：2026-09-21，更新于 2026-09-22。文件选择、正式名称版本替换、数据库约束和浏览器验收已完成。工作区原有其他按钮/布局修改保留；文书下载按钮样式随此次下载流程改造保留。

## 已实施

- 决议与修正案正文只选择本委员会 `PUBLISHED` 文件。去除文书页面上传、提交审核及发布调用。候选显示名称与现有类型，超过 10 个候选才启用搜索。
- 加载、失败、空列表分别显示；委员会事件更新列表并清除失效候选。提交期间阻止重复操作，保存失败保留候选并可重试；正文引用只随成功后的快照更新。未编辑正文时切换显示模式、失焦不会创建新版本。
- 下载复用准备下载、查询状态、下载流程，保留蓝色向下箭头按钮。
- 新文书版本按现有权限、revision、冻结规则校验，然后在事务内锁定同委员会的文件，拒绝未发布或已删除文件，返回 `DOCUMENT_FILE_NOT_PUBLISHED`。
- 网页文件批准、驳回、发布及删除改为先锁委员会、再锁文件，匹配文书绑定与 provider commit 的顺序。没有新增被文书引用就禁止删除的规则。
- Stage 4 和 Stage 5 保留已删除/未发布引用及文件类型，引用仍跟随文件记录的当前版本。不可用文件不会变成空文本正文。
- 已同步 `PROJECT_ARCHITECTURE.md`。

## 当前验证

- 页面路由与存储定向测试：3 文件、97 项通过。
- `pnpm build:self-host`：通过，包含浏览器、契约、后端、规则及 Node Agent 构建。
- 真实 PostgreSQL：使用仓库 PostgreSQL 16 测试服务与随机临时数据库。运行 `pnpm test:self-host:integration --testNamePattern 'keeps the motion and resolution unchanged until an attached body file is published' --maxWorkers=2 --minWorkers=1`，1 项通过，126 项因名称筛选未运行。覆盖未发布文件拒绝、现有发布/展示规则、决议和修正案引用、越权拒绝、文件新版本状态、实际删除接口与绑定并发、删除后引用保留及绑定拒绝。
- 不配置 `TEST_DATABASE_ADMIN_URL` 的运行明确跳过；另一次已配置并获准连接本机数据库的运行提供上述真实数据库证据。
- 全仓 `pnpm test:self-host` 曾出现原生 desktop-bridge 子进程多次 30 秒超时，以及计时器测试超时/失败，已中止，不能视为全仓通过；随后本阶段相关 97 项测试独立运行全部通过。
- 静态核对：`ProceedingsPanel.tsx` 不再包含本地文件输入、上传、提交审核或批准文件的调用。
- 本机开发库已发布文件重名预查：委员会内精确名称、`lower(btrim(logical_name))` 两种分组均为零冲突。后者仅用于风险预查，不代表已决定名称规则。

## 名称与客户端规则（2026-09-22 实施）

用户要求继续实施并自行推进，采用以下明确规则：首尾空白按 JavaScript trim 去除，名称区分大小写，不模糊匹配；已删除名称允许新文件复用，旧引用仍不可用；主席客户端修改正式文件产生需要审核的冲突，引导网页上传审核，保留同步功能。

## 正式文件版本已实施

- schema 66 保存正式名称并施加委员会内非删除正式文件唯一约束；迁移先报告重名 ID 与文书引用并回滚。
- 预览及二次确认绑定源提交、内容、正式名称、类型、目标记录及双方 revision；原子批准追加目标版本，复用原 blob，保留提交及审核来源。
- 已合并记录保留为提交历史，不在正式列表重复出现。文书及下载解析原引用，继续使用目标当前版本；终态文书状态与结果不变。
- 正式文件网页直接覆盖及客户端本地更新不能绕过审核；新增 REVIEW_REQUIRED 冲突。Chair 上传保存为独立路径，缓存回填追踪实际持久来源。
- 删除正式文件仍被允许，同时清理合并提交关联的字节；没有新增被引用就禁止删除的限制。
- 中英文居中替换确认、同名提示、取消、过期重试及重复提交保护已接入。
- 表决入口补充文件可用状态复核。

## 最终验证（2026-09-22）

- `pnpm test:self-host --maxWorkers=2 --minWorkers=1 --testTimeout=30000`：88 个文件、664 项全部通过。此前固定冲突类型列表缺少新枚举的失败已修正；源码测试不是实际设备证据。
- `pnpm test:self-host:integration --maxWorkers=2 --minWorkers=1 --testTimeout=30000 --hookTimeout=30000`：9 个文件、131 项全部通过。使用 PostgreSQL 16 随机临时数据库。默认五秒时限曾因本机负载产生无关超时，最终运行使用上述时限。
- 最后补充删除引用读取最新原始名称/类型后，数据库定向回归 3 项通过；文件类型语言统一后，页面路由 86 项通过。
- 完整 self-host 构建通过；最终部署构建及健康记录见下。
- 静态核对：两处正文页没有本地文件输入、上传、提交审核或发布文件调用。

## 浏览器验收

实际通过 Codex 浏览器访问 Vite 和已升级的 quorum-dev API；保持既有权限和登录身份。中文与英文分别检查，窄屏设置为 390×844、320×800（当前浏览器缩放下内容区约 416/355 CSS px），无水平溢出。验收后恢复默认尺寸与中文界面。

1. 文件模块上传 proposal.txt，独立待审核记录成功持久保存；尚未发布时决议显示“暂无已发布文件”，没有上传/审核控件。
2. 文本正文正常保存；切换文件再切回文本，原文保留。批准正式文件后，决议及修正案选择器显示它及类型；空选择不可保存，点击“使用此文件”后绑定成功。
3. 再上传相同原始名称的不同内容，保持独立提交。审核输入正式名称立即显示同名提示；中文居中二次确认取消后保留提交及名称；英文重新打开确认并成功替换。
4. 目标 `8ba45bea-64b1-466a-823a-b1436b1fb7b1` 从版本 1 变为 2，总览只有一条正式文件，源提交以 `merged_into_file_entry_id` 保留。新版本 SHA-256 为 `9c56949952dfa06309c35153daeaf1675e9af7102c51d3c478aaf888f5189321`，与本地替换上传文件相同。
5. 决议 `ff0b6ebf-d431-4802-b03b-670203eba68d` 和修正案 `52e09112-29e9-4bfe-b2d4-6b6280b24503` 均引用该目标。为验收终态，使用已授权的本机测试数据设置其为 PASSED/INCORPORATED；替换前后状态及 revision（6/3）不变。终态页面没有改选控件，仍可触发下载。
6. 通过文件总览删除目标成功；决议和修正案保留文件名、删除状态及不可用提示，没有下载按钮，没有伪装为空文本。中文窄屏亦确认。
7. 浏览器截图保存在 `/tmp/quorum-file-acceptance/screenshots/replacement-zh.png` 与 `replacement-en.png`。候选加载失败、重试、事件过期与越权由组件/数据库用例覆盖，未将这些用例称为浏览器验收。

## 存储验证边界

- SERVER_VOLUME：真实数据库、实际磁盘上传/下载/删除，以及已部署服务的浏览器操作。
- S3_COMPATIBLE：真实 PostgreSQL 与模拟 S3 传输，验证复用 blob、事务回滚、版本归属和清理；没有使用真实外部 S3 服务。
- CHAIR_AGENT：真实 PostgreSQL 与模拟 Agent 协议任务，验证独立持久路径、在线缓存回填、离线缓存命中、离线无缓存不可用及拒绝客户端绕过；没有用实际主席客户端设备执行本轮验收。
- 委员会永久删除测试通过，未改变整体永久删除流程。

## 部署

使用 `docker compose -p quorum-dev --env-file deploy/.env -f deploy/compose.yaml build app caddy` 构建，并使用 `up -d --no-deps app caddy` 替换运行容器。数据库与存储卷保留。已确认新实例 `/health/ready` 返回 HTTP 200，数据库 migrationVersion 为 66，存储健康。

最终浏览器收尾时补充的文件类型语言与删除引用元数据修正继续构建、更新同一实例；最终镜像及健康核验随收尾提交记录。
