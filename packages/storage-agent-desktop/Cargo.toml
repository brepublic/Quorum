[package]
name = "quorum-storage-agent-desktop"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
slint = { version = "=1.17.1", default-features = false, features = ["std", "backend-winit", "renderer-software", "compat-1-2"] }
serde_json = "1"
rfd = "0.15"

[build-dependencies]
slint-build = "=1.17.1"

[profile.release]
lto = "thin"
strip = true
